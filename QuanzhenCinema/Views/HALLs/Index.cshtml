@model IEnumerable<QuanzhenCinema.Models.HALL>

@{
    ViewBag.Title = "Index";
}

<h2>索引</h2>

<p>
    @Html.ActionLink("创建", "Create")
</p>
<table class="table">
    <tr>
        <th>
            容量
        </th>
        <th>
            3D
        </th>
        <th>
            杜比
        </th>
        <th>
            巨幕
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CAPACITY)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IS_3D)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IS_DOLAB)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IS_IMAX)
        </td>
        <td>
            @Html.ActionLink("编辑", "Edit", new { id=item.HALL_ID }) |
            @Html.ActionLink("详情", "Details", new { id=item.HALL_ID }) |
            @Html.ActionLink("删除", "Delete", new { id=item.HALL_ID })
        </td>
    </tr>
}

</table>
