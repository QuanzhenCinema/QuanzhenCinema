@model IEnumerable<QuanzhenCinema.Models.DISPLAY>

@{
    ViewBag.Title = "Index";
}

<h2>索引</h2>

<p>
    @Html.ActionLink("创建", "Create")
</p>
<table class="table">
    <tr>
        <th>
            放映号
        </th>
        <th>
            电影名
        </th>
        <th>
            3D
        </th>
        <th>
            巨幕
        </th>
        <th>
            语言
        </th>
        <th>
            最低价
        </th>
        <th>
            片长
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DISPLAY_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MOVIE.NAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IS_3D)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IS_IMAX)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LANGUAGE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LOWEST_PRICE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LENGTH)
        </td>
        <td>
            @Html.ActionLink("编辑", "Edit", new { id=item.DISPLAY_ID }) |
            @Html.ActionLink("详情", "Details", new { id=item.DISPLAY_ID }) |
            @Html.ActionLink("删除", "Delete", new { id=item.DISPLAY_ID })
        </td>
    </tr>
}

</table>
